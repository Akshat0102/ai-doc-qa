# AI Document Q&A

This is an application that allows users to upload documents and ask questions about their content. The system uses a Retrieval-Augmented Generation (RAG) pipeline to process documents, generate embeddings, store them in a vector database, and provide intelligent answers to user queries using OpenAI's GPT model.